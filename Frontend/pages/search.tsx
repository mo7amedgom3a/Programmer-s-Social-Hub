import React from "react";
import SearchPage from "../app/components/search"; // Assuming SearchPage is in the components folder

const Search = () => {
  return <SearchPage />;
};

export default Search;