import React from "react";
import Home from "../../app/components/Home"; // Assuming Home component is in the components folder

const HomePage = () => {
  return <Home />;
};

export default HomePage;